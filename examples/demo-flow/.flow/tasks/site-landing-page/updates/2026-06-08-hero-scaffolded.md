# Landing page — hero scaffolded, pricing block in

Devin scaffolded the hero and the $8/mo pricing block. Layout is responsive down
to mobile and the page loads fast — kept it a static export so it doesn't pull in
the app bundle.

The copy is placeholder for now. Real product copy is waiting on Priya's brand
mocks (tracked in site-product-copy), so the words and final visual polish come
after that.

Blocked on: brand mocks from Priya (for the copy pass).
