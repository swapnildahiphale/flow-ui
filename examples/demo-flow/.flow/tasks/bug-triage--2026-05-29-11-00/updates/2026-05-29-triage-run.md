# Bug triage — 2026-05-29

Quiet week — three new reports. The confetti-rs zero-particle panic is fixed and
verified, so it dropped off the list. Remaining: a flaky chart tooltip on the
roadmap page (low) and a sign-in redirect loop one beta user hit intermittently
(couldn't reproduce — labeled `#bug`, asked for repro steps, parked).

No new P0s. Top fix for next cycle stays the timezone rollup fix; everything
else is cosmetic or unreproduced.
