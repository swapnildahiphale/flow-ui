# Tideline — beta at 120 users; billing is the gate to GA

The beta sits at ~120 users and engagement is healthy — the streak dashboard is
landing and the timezone fix is in review, so the core analytics will finally be
correct. Confetti-rs WASM is close, which unlocks the celebration moment.

The one thing standing between here and GA is billing. Stripe is wired but
blocked on Stripe support allowlisting our webhook endpoint. Everything else
(OAuth, onboarding) can follow once we can actually charge.

Blocked on: Stripe support (webhook allowlist).
